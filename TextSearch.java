package utilities;
/** This object receives a comma separated file and a file extension. It then
 *  collects all terms inside of the comma separated file with that
 *  extension. The first argument is the comma separated file, the second
 *  variable is the file extension of interest.*/
public class TextSearch {
    public static void main(String[] args) {
        
    }

}
